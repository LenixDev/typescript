{
  "name": "lenix-fivem-typescript-template",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tsc --build",
    "watch": "npx tsc --build --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@citizenfx/client": "latest",
    "@citizenfx/server": "latest"
  }
}